import { AbiItem } from 'web3-utils';

export const SyncChain = require("../../abi/SyncChain.json") as AbiItem[];
export const HostChain = require("../../abi/HostChain.json") as AbiItem[];
export const IChain = require("../../abi/IChain.json") as AbiItem[];